@model GymFinal.Models.Plan
@{
    ViewData["Title"] = "Editar Plan";
}

<!-- Misma estructura visual que Create -->
<!-- Solo cambia el título y mantiene el mismo formulario -->
@await Html.PartialAsync("Create")
